# Linux-specific requirements for RotMG Bot
# Core dependencies
PySide6>=6.0.0
opencv-python>=4.5.0
numpy>=1.21.0
mss>=6.1.0
pynput>=1.7.0
Pillow>=8.0.0

# Linux-specific dependencies
python-xlib>=0.33
evdev>=1.6.0

# Optional: for better window management
# xdotool (install via system package manager: sudo apt install xdotool) 